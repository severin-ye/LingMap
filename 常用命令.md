

# tree
```
tree -I '__pycache__|output|logs'
```

# 运行测试
```
# 运行所有测试
python -m tests.run_all_tests

# 运行特定阶段测试
python -m tests.stage_1.run_tests
python -m tests.stage_2.run_tests
python -m tests.stage_3.run_tests
python -m tests.stage_4.run_tests
python -m tests.stage_5.run_tests
python -m tests.stage_6.run_tests

# 使用pytest运行特定测试
pytest tests/api_tests/test_api_integration.py -v
```

# 系统运行
```
# 运行完整系统处理
python main.py --novel_path novel/1.txt

# 使用自定义配置运行系统
python main.py --novel_path novel/test.txt --config_path common/config/config.json
```

# 并行配置分析
```
# 检查并行化配置
python scripts/unified_parallel_tool.py analyze
```